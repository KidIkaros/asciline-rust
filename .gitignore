/target
Cargo.lock.orig
# Rust-generated differential vectors (regenerated by the tests themselves);
# the Python-generated inputs (vectors_python.bin, vectors_profile_py.bin) ARE
# committed so CI runs the harnesses hermetically without numpy / the original repo.
experiments/vectors_rust.bin
experiments/vectors_profile_rust.bin
experiments/gen_python.log
*.ascf
*.mp3
# cargo-fuzz outputs (fuzz/corpus/ seeds ARE committed)
fuzz/artifacts
fuzz/target

# Python bytecode
__pycache__/
*.pyc
