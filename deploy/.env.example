# Copy to .env and edit. Used by docker-compose.yml in this directory.
# Optional auth: when set, /ws, /audio and /scrub* require ?token=<value>.
# Leave empty for localhost-only deployments.
ASCILINE_TOKEN=
RUST_LOG=info
